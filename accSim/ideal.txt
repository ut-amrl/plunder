ACC -> DEC
xToTarget - r.DistTraveled(state.vel, r.decMax) < 0

ACC -> CON
state.vel - r.vMax > 0

DEC -> ACC
false

DEC -> CON
false

CON -> ACC
false

CON -> DEC
xToTarget - r.DistTraveled(state.vel, r.decMax) < 0
